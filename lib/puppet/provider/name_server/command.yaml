---
get_values: 
  default: 'show running-config all | include ip name-server'
get_instances:
  default: 'ip name-server.+\\n'
set_values:
  default: '<state> ip name-server <name>'
ensure_is_state:
  default: 'true'
attributes:
  name:
    default:
      get_value: 'ip name-server (?<name>\S*)'
