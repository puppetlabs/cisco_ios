---
get_values:
  default: 'show running-config | include ^ip tacacs source-interface|^tacacs-server'
attributes:
  name:
    default:
      get_value: 'default'
  enable:
    default:
      get_value: '<enable> tacacs'
      set_value: "<enable> tacacs"
