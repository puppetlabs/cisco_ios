---
get_values:
  default: 'show running-config | include ^ip tacacs source-interface|^tacacs-server'
attributes:
  enable:
    default:
      get_value: '(?:(?:tacacs-server key (?:\S )?)(?:(?<key_value>\S*))).*'
      set_value: "<state>tacacs-server key <key_format><key_value>\n"
